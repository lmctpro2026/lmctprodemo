import Component from "@/components/ui/saa-s-template"

export const metadata = {
  title: "LMCT PRO — alt landing (v2)",
  description: "Dark-mode SaaS direction. Use to compare against the editorial landing at /.",
  // Prevent search engines from indexing the comparison page.
  robots: { index: false, follow: false },
}

export default function V2Page() {
  return <Component />
}
